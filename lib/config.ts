/**\n * Application Configuration\n * Centralized contact and business information\n */\n\nexport const BUSINESS_CONFIG = {\n  name: process.env.NEXT_PUBLIC_BUSINESS_NAME || 'Jimora Africa Adventures',\n  email: process.env.NEXT_PUBLIC_CONTACT_EMAIL || 'info@jimoraafrica.co.ke',\n  phone: {\n    primary: process.env.NEXT_PUBLIC_PHONE_1 || '+254 711 734 900',\n    secondary: process.env.NEXT_PUBLIC_PHONE_2 || '+254 716 814 666',\n  },\n  location: process.env.NEXT_PUBLIC_LOCATION || 'Mombasa, Nairobi & East Africa, Kenya',\n  businessHours: {\n    weekdays: 'Mon - Fri: 8:00 AM - 6:00 PM',\n    saturday: 'Saturday: 9:00 AM - 4:00 PM',\n    sunday: 'Sunday: Closed',\n  },\n  website: 'jimoraafrica.co.ke',\n} as const;\n\n/**\n * Get contact information\n */\nexport function getContactInfo() {\n  return {\n    email: BUSINESS_CONFIG.email,\n    phone: BUSINESS_CONFIG.phone.primary,\n    alternatePhone: BUSINESS_CONFIG.phone.secondary,\n    address: BUSINESS_CONFIG.location,\n  };\n}\n\n/**\n * Format phone number for display\n */\nexport function formatPhoneNumber(phone: string): string {\n  return phone;\n}\n\n/**\n * Validate email format\n */\nexport function isValidEmail(email: string): boolean {\n  const emailRegex = /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/;\n  return emailRegex.test(email);\n}\n"